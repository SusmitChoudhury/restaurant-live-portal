{
  "name": "restaurant-live-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "node server/server.js",
    "client": "npm run dev --prefix client",
    "build": "npm run build --prefix client"
  }
}
